<div class="date-confirmation">
	<h3>It's on!</h3>
	<p>You have a date with <%= @sortie.party_of(current_user).first_name %>, <em><%= @sortie.time.strftime("%A at %I:%M%P") %></em>.</p>
	<div class="people">
		<div>
			<img src="<%= @sortie.host.picture_url(:thumb) %>" />
			<%= @sortie.host.first_name %>
		</div>
		<div>
			<img src="<%= @sortie.guest.picture_url(:thumb) %>" />
			<%= @sortie.guest.first_name %>
		</div>
	</div>
	<div class="location">
		<div class="map">
			<img src="http://maps.google.com/maps/api/staticmap?zoom=13&amp;size=400x400&amp;markers=<%= "#{@sortie.place.lat},#{@sortie.place.lng}" %>&amp;sensor=false" alt="map" />
		</div>
		<div class="place">
			<div class="title"><%= @sortie.place.name %></div>
			<div class="address">
				<%= @sortie.place.address %><br />
				<%= @sortie.place.city %>, <%= @sortie.place.state_code %>
			</div>
		</div>
	</div>
	<div class="info">You will both be able to chat by SMS 2 hours before the date so you can arrange last minute details :-)</div>
	<p>Good luck!</p>
</div>
